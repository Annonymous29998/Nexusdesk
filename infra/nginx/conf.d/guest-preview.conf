# Social / chat link preview detection (WhatsApp, iMessage, Slack, etc.).
map $http_user_agent $social_crawler {
  default 0;
  ~*(facebookexternalhit|WhatsApp|Twitterbot|LinkedInBot|Slackbot|Discordbot|TelegramBot|facebookcatalog|Pinterest) 1;
}
